<Window x:Class="WpfAppCalculator.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Калькулятор выражения" Height="350" Width="400">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <TextBlock Grid.Row="0" Grid.ColumnSpan="2" Text="Вычисление выражения: e^x - x - 2 + (1 + x)^x" 
                  Margin="0,0,0,10" FontWeight="Bold" TextAlignment="Center"/>

        <TextBlock Grid.Row="1" Grid.Column="0" Text="Введите значение x:" Margin="0,0,10,10"/>
        <TextBox Grid.Row="1" Grid.Column="1" Name="txtX" Margin="0,0,0,10" Text="4"/>

        <Button Grid.Row="2" Grid.ColumnSpan="2" Name="btnCalculate" Content="Вычислить" 
                Margin="0,0,0,10" Height="30" Click="btnCalculate_Click"/>

        <TextBlock Grid.Row="3" Grid.Column="0" Text="Результат:" Margin="0,0,10,10"/>
        <TextBlock Grid.Row="3" Grid.Column="1" Name="txtResult" Margin="0,0,0,10"/>

        <TextBlock Grid.Row="4" Grid.Column="0" Text="История вычислений:" Margin="0,0,10,10"/>
        <ListBox Grid.Row="5" Grid.ColumnSpan="2" Name="lstHistory" Margin="0"/>
    </Grid>
</Window>